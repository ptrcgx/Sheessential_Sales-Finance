@model Sheessential_Sales_Finance.Models.ExpensesWithBalanceViewModel

@{
    ViewData["Title"] = "Expense Management";
}

<link href="~/css/output.css" rel="stylesheet" />

<div class="p-6 bg-gradient-to-br from-gray-50 to-gray-100 min-h-screen">
    <!-- HEADER -->
    <div class="flex justify-between items-center mb-8">
        <div>
            <h1 class="text-4xl font-bold text-gray-900 mb-1">Expense Management</h1>
            <p class="text-gray-600">Manage and track company expense requests</p>
        </div>
        <button id="openModalBtn" class="bg-gradient-to-r from-blue-600 to-blue-700 text-white px-6 py-3 rounded-xl hover:from-blue-700 hover:to-blue-800 transition-all shadow-lg hover:shadow-xl transform hover:-translate-y-0.5 flex items-center gap-2">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
            </svg>
            New Expense Request
        </button>
    </div>

    <!-- SUMMARY CARDS -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-5 gap-6 mb-8">
        <!-- Available Balance Card -->
        <div class="bg-white p-6 rounded-2xl shadow-md hover:shadow-lg transition-shadow border border-gray-100">
            <div class="flex items-center gap-4 mb-2">
                <div class="w-10 h-10 bg-gray-100 rounded-lg flex items-center justify-center">
                    <svg class="w-5 h-5 text-blue-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 6l3 1m0 0l-3 9a5.002 5.002 0 006.001 0M6 7l3 9M6 7l6-2m6 2l3-1m-3 1l-3 9a5.002 5.002 0 006.001 0M18 7l3 9m-3-9l-6-2m0-2v2m0 16V5m0 16H9m3 0h3"></path>
                    </svg>
                </div>
                
                <p class="text-blue-500 text-sm font-medium">Available Balance</p>
            </div>

            @if (Model.Balance != null)
            {
                <h3 class="text-3xl font-bold text-gray-900 mt-1">
                    @Model.Balance.CurrentBalance.ToString("C")
                </h3>
                <p class="text-gray-500 text-sm mt-1">All time</p>
            }
            else
            {
                <h3 class="text-3xl font-bold text-gray-900 mt-1">
                    ₱0.00
                </h3>
            }
        </div>

        <div class="bg-white p-6 rounded-2xl shadow-md hover:shadow-lg transition-shadow border border-gray-100">
            <div class="flex items-center gap-2 mb-2">
                <div class="w-10 h-10 bg-gray-100 rounded-lg flex items-center justify-center">
                    <svg class="w-6 h-6 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                </div>
                <p class="text-gray-600 text-sm font-medium">Total Expenses</p>
            </div>
            <h2 class="text-2xl font-bold text-gray-900">₱@ViewBag.TotalExpenses.ToString("N2")</h2>
            <p class="text-gray-500 text-sm mt-1">All time</p>
        </div>

        <div class="bg-white p-6 rounded-2xl shadow-md hover:shadow-lg transition-shadow border border-gray-100">
            <div class="flex items-center gap-2 mb-2">
                <div class="w-10 h-10 bg-yellow-100 rounded-lg flex items-center justify-center">
                    <svg class="w-6 h-6 text-yellow-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                </div>
                <p class="text-gray-600 text-sm font-medium">Pending</p>
            </div>
            <h2 class="text-2xl font-bold text-yellow-600">₱@ViewBag.PendingTotal.ToString("N2")</h2>
            <p class="text-gray-500 text-sm mt-1">Awaiting approval</p>
        </div>

        <div class="bg-white p-6 rounded-2xl shadow-md hover:shadow-lg transition-shadow border border-gray-100">
            <div class="flex items-center gap-2 mb-2">
                <div class="w-10 h-10 bg-green-100 rounded-lg flex items-center justify-center">
                    <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                </div>
                <p class="text-gray-600 text-sm font-medium">Approved</p>
            </div>
            <h2 class="text-2xl font-bold text-green-600">₱@ViewBag.ApprovedTotal.ToString("N2")</h2>
            <p class="text-gray-500 text-sm mt-1">This month</p>
        </div>

        <div class="bg-white p-6 rounded-2xl shadow-md hover:shadow-lg transition-shadow border border-gray-100">
            <div class="flex items-center gap-2 mb-2">
                <div class="w-10 h-10 bg-red-100 rounded-lg flex items-center justify-center">
                    <svg class="w-6 h-6 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                </div>
                <p class="text-gray-600 text-sm font-medium">Declined</p>
            </div>
            <h2 class="text-2xl font-bold text-red-600">₱@ViewBag.DeclinedTotal.ToString("N2")</h2>
            <p class="text-gray-500 text-sm mt-1">This month</p>
        </div>
    </div>

    <!-- Balance Alert -->
    <div id="balanceAlert" class="bg-yellow-50 border-l-4 border-yellow-400 p-4 mb-6 rounded-lg shadow-sm hidden">
        <div class="flex items-center">
            <svg class="w-5 h-5 text-yellow-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
            </svg>
            <div>
                <p class="text-yellow-800 font-medium">Low Balance Warning</p>
                <p class="text-yellow-700 text-sm">Available balance is below ₱20,000. Consider adding funds before approving large expenses.</p>
            </div>
        </div>
    </div>

    <!-- EXPENSE REQUESTS TABLE -->
    <div class="bg-white rounded-2xl shadow-md overflow-hidden">
        <div class="p-6 border-b border-gray-200 bg-gradient-to-r from-gray-50 to-white">
            <div class="flex justify-between items-center">
                <div>
                    <h2 class="text-2xl font-bold text-gray-900">Expense Requests</h2>
                    <p class="text-gray-600 text-sm mt-1">Review and manage company expenses</p>
                </div>
              
                <!-- Filter Button -->
                <div class="relative inline-block text-left">
                    <button id="filterButton"
                            type="button"
                            class="px-4 py-2 border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors flex items-center gap-2">
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                  d="M3 4a1 1 0 011-1h16a1 1 0 011 1v2.586a1 1 0 01-.293.707l-6.414 6.414a1 1 0 00-.293.707V17l-4 4v-6.586a1 1 0 00-.293-.707L3.293 7.293A1 1 0 013 6.586V4z" />
                        </svg>
                        Filter
                    </button>
                </div>

                <!-- Filter Dropdown (now fixed position) -->
                <div id="filterDropdown"
                     class="hidden fixed bottom-[40px] right-[40px] w-72 bg-white border border-gray-200 rounded-xl shadow-2xl p-4 z-[9999] animate-fadeIn">
                    <form method="get" asp-action="Expenses" class="space-y-3">
                        <!-- Status Filter -->
                        <div>
                            <label class="block text-sm font-medium text-gray-700 mb-1">Status</label>
                            <select name="status"
                                    class="w-full border border-gray-300 rounded-md p-2 text-sm focus:ring focus:ring-blue-200">
                                <option value="">All</option>
                                <option value="Pending" selected>Pending</option>
                                <option value="Approved">Approved</option>
                                <option value="Declined">Declined</option>
                            </select>
                        </div>

                        <!-- Date Filter -->
                        <div class="grid grid-cols-2 gap-2">
                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-1">From</label>
                                <input type="date" name="startDate"
                                       class="w-full border border-gray-300 rounded-md p-2 text-sm">
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-gray-700 mb-1">To</label>
                                <input type="date" name="endDate"
                                       class="w-full border border-gray-300 rounded-md p-2 text-sm">
                            </div>
                        </div>

                        <div class="flex justify-end gap-2 mt-3">
                            <button type="submit"
                                    class="px-4 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600 text-sm">
                                Apply
                            </button>
                        </div>
                    </form>
                </div>


                </div>

            </div>
        </div>

        <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-gray-600 uppercase tracking-wider">Expense ID</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-gray-600 uppercase tracking-wider">Requester</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-gray-600 uppercase tracking-wider">Department</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-gray-600 uppercase tracking-wider">Type</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-gray-600 uppercase tracking-wider">Amount</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-gray-600 uppercase tracking-wider">Description</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-gray-600 uppercase tracking-wider">Status</th>
                        <th class="px-6 py-4 text-left text-xs font-semibold text-gray-600 uppercase tracking-wider">Actions</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200">
                    @if (Model != null && Model.Expenses != null && Model.Expenses.Any())
                    {
                        @foreach (var expense in Model.Expenses)
                        {
                            <tr class="hover:bg-gray-50 transition-colors">
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <span class="text-sm font-medium text-blue-600">@expense.ExpenseId</span>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <div class="flex items-center">
                                        <div class="w-8 h-8 bg-gradient-to-br from-blue-500 to-blue-600 rounded-full flex items-center justify-center text-white text-xs font-medium mr-3">
                                            @expense.RequestedBy.Substring(0, 1).ToUpper()
                                        </div>
                                        <span class="text-sm text-gray-900">@expense.RequestedBy</span>
                                    </div>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <span class="px-3 py-1 inline-flex text-xs leading-5 font-medium rounded-full bg-gray-100 text-gray-800">
                                        @expense.Department
                                    </span>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-700">@expense.ExpenseType</td>
                                <td class="px-6 py-4 whitespace-nowrap">
                                    <span class="text-sm font-semibold text-gray-900">₱@expense.Amount.ToString("N2")</span>
                                </td>
                                <td class="px-6 py-4 max-w-xs">
                                    <p class="text-sm text-gray-600 truncate">@expense.Description</p>
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap">
                                    @if (expense.Status == "Pending")
                                    {
                                        <span class="px-3 py-1 inline-flex items-center text-xs leading-5 font-semibold rounded-full bg-yellow-100 text-yellow-800">
                                            <span class="w-2 h-2 bg-yellow-500 rounded-full mr-2 animate-pulse"></span>
                                            Pending
                                        </span>
                                    }
                                    else if (expense.Status == "Approved")
                                    {
                                        <span class="px-3 py-1 inline-flex items-center text-xs leading-5 font-semibold rounded-full bg-green-100 text-green-800">
                                            <svg class="w-3 h-3 mr-1" fill="currentColor" viewBox="0 0 20 20">
                                                <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"></path>
                                            </svg>
                                            Approved
                                        </span>
                                    }
                                    else if (expense.Status == "Declined")
                                    {
                                        <span class="px-3 py-1 inline-flex items-center text-xs leading-5 font-semibold rounded-full bg-red-100 text-red-800">
                                            <svg class="w-3 h-3 mr-1" fill="currentColor" viewBox="0 0 20 20">
                                                <path fill-rule="evenodd" d="M4.293 4.293a1 1 0 011.414 0L10 8.586l4.293-4.293a1 1 0 111.414 1.414L11.414 10l4.293 4.293a1 1 0 01-1.414 1.414L10 11.414l-4.293 4.293a1 1 0 01-1.414-1.414L8.586 10 4.293 5.707a1 1 0 010-1.414z" clip-rule="evenodd"></path>
                                            </svg>
                                            Declined
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="px-3 py-1 inline-flex text-xs leading-5 font-semibold rounded-full bg-gray-100 text-gray-800">@expense.Status</span>
                                    }
                                </td>
                                <td class="px-6 py-4 whitespace-nowrap">
                                    @if (expense.Status == "Pending")
                                    {
                                        <button type="button" 
                                                class="view-details-btn px-4 py-2 bg-gradient-to-r from-blue-600 to-blue-700 text-white text-sm font-medium rounded-lg hover:from-blue-700 hover:to-blue-800 transition-all shadow-sm hover:shadow-md transform hover:-translate-y-0.5"
                                                data-id="@expense.Id"
                                                data-expense-id="@expense.ExpenseId"
                                                data-requester="@expense.RequestedBy"
                                                data-department="@expense.Department"
                                                data-type="@expense.ExpenseType"
                                                data-amount="@expense.Amount.ToString("N2")"
                                                data-description="@expense.Description"
                                                data-notes="@expense.Notes"
                                                data-status="@expense.Status">
                                            Review
                                        </button>
                                    }
                                    else
                                    {
                                        <span class="px-4 py-2 bg-gray-100 text-gray-500 text-sm font-medium rounded-lg cursor-not-allowed inline-block">
                                            @expense.Status
                                        </span>
                                    }
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="8" class="px-6 py-12 text-center">
                                <div class="flex flex-col items-center">
                                    <svg class="w-16 h-16 text-gray-300 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                                    </svg>
                                    <p class="text-gray-500 text-lg font-medium">No expense requests found</p>
                                    <p class="text-gray-400 text-sm mt-1">Create a new expense request to get started</p>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>


<!-- PREVIEW/DETAILS MODAL -->
<div id="previewModal" class="fixed inset-0 bg-white bg-opacity-60 hidden items-center justify-center z-50 backdrop-blur-sm">
    <div class="bg-white w-full max-w-3xl rounded-3xl shadow-2xl p-8 max-h-[90vh] overflow-y-auto m-4 transform transition-all">
        <div class="flex justify-between items-center mb-6 pb-4 border-b border-gray-200">
            <div class="flex items-center gap-3">
                <div class="w-12 h-12 bg-blue-100 rounded-xl flex items-center justify-center">
                    <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                    </svg>
                </div>
                <div>
                    <h2 class="text-2xl font-bold text-gray-900">Expense Request Details</h2>
                    <p class="text-gray-500 text-sm">Review and take action</p>
                </div>
            </div>
            <button id="closePreviewBtn" class="text-gray-400 hover:text-gray-600 transition-colors">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                </svg>
            </button>
        </div>

        <div class="space-y-5">
            <!-- Expense ID -->
            <div class="bg-gradient-to-br from-blue-50 to-blue-100 p-5 rounded-xl border border-blue-200">
                <p class="text-sm text-blue-700 font-medium mb-1">Expense ID</p>
                <p class="text-xl font-bold text-blue-900" id="preview-expense-id">EXP-001</p>
            </div>

            <!-- Requester Info -->
            <div class="grid grid-cols-2 gap-4">
                <div class="bg-gray-50 p-5 rounded-xl border border-gray-200">
                    <p class="text-sm text-gray-600 font-medium mb-1">Requested By</p>
                    <div class="flex items-center gap-2">
                        <div class="w-8 h-8 bg-gradient-to-br from-blue-500 to-blue-600 rounded-full flex items-center justify-center text-white text-xs font-medium">
                            <span id="preview-requester-initial">J</span>
                        </div>
                        <p class="text-lg font-semibold text-gray-900" id="preview-requester">John Doe</p>
                    </div>
                </div>
                <div class="bg-gray-50 p-5 rounded-xl border border-gray-200">
                    <p class="text-sm text-gray-600 font-medium mb-1">Department</p>
                    <p class="text-lg font-semibold text-gray-900" id="preview-department">Marketing</p>
                </div>
            </div>

            <!-- Expense Type & Amount -->
            <div class="grid grid-cols-2 gap-4">
                <div class="bg-gray-50 p-5 rounded-xl border border-gray-200">
                    <p class="text-sm text-gray-600 font-medium mb-1">Expense Type</p>
                    <p class="text-lg font-semibold text-gray-900" id="preview-type">Office Supplies</p>
                </div>
                <div class="bg-gradient-to-br from-green-50 to-emerald-50 p-5 rounded-xl border border-green-200">
                    <p class="text-sm text-green-700 font-medium mb-1">Amount Requested</p>
                    <p class="text-2xl font-bold text-green-700" id="preview-amount">₱5,000.00</p>
                </div>
            </div>

            <!-- Description -->
            <div class="bg-gray-50 p-5 rounded-xl border border-gray-200">
                <p class="text-sm text-gray-600 font-medium mb-2">Description</p>
                <p class="text-gray-900 leading-relaxed" id="preview-description">Purchase of office supplies for Q4</p>
            </div>

            <!-- Notes -->
            <div class="bg-gray-50 p-5 rounded-xl border border-gray-200">
                <p class="text-sm text-gray-600 font-medium mb-2">Additional Notes</p>
                <p class="text-gray-700 leading-relaxed" id="preview-notes">Urgent request for marketing campaign materials</p>
            </div>

            <!-- Status -->
            <div class="bg-yellow-50 p-5 rounded-xl border-l-4 border-yellow-500">
                <div class="flex items-center gap-2 mb-1">
                    <svg class="w-5 h-5 text-yellow-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                    <p class="text-sm text-yellow-700 font-medium">Current Status</p>
                </div>
                <p class="text-lg font-bold text-yellow-700" id="preview-status">Pending Review</p>
            </div>

            <!-- Balance Check Info -->
            <div id="balanceCheckInfo" class="bg-blue-50 p-5 rounded-xl border border-blue-200">
                <div class="flex items-start gap-3">
                    <svg class="w-5 h-5 text-blue-600 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                    <div class="flex-1">
                        <p class="text-sm text-blue-800 font-medium mb-2">Balance Information</p>
                        <div class="space-y-1 text-sm text-blue-700">
                            <p>Current Available Balance: <span class="font-semibold">₱50,000.00</span></p>
                            <p>After Approval: <span class="font-semibold" id="balance-after-approval">₱45,000.00</span></p>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Action Buttons -->
        <div class="flex justify-end gap-3 mt-8 pt-6 border-t border-gray-200">
            <button type="button" id="cancelPreviewBtn" class="px-6 py-2.5 bg-gray-100 text-gray-700 rounded-xl hover:bg-gray-200 transition-colors font-medium">
                Cancel
            </button>
            
            <button type="button" id="declineBtn" class="px-6 py-2.5 bg-gradient-to-r from-red-500 to-red-600 text-white rounded-xl hover:from-red-600 hover:to-red-700 transition-all shadow-md hover:shadow-lg transform hover:-translate-y-0.5 font-medium flex items-center gap-2">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                </svg>
                Decline Request
            </button>
            
            <button type="button" id="approveBtn" class="px-6 py-2.5 bg-gradient-to-r from-green-500 to-green-600 text-white rounded-xl hover:from-green-600 hover:to-green-700 transition-all shadow-md hover:shadow-lg transform hover:-translate-y-0.5 font-medium flex items-center gap-2">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                Approve & Transfer
            </button>
        </div>
    </div>
</div>


<!-- DECLINE REASON MODAL -->
<div id="declineReasonModal" class="fixed inset-0 bg-white bg-opacity-60 hidden items-center justify-center z-50 backdrop-blur-sm">
    <div class="bg-white w-full max-w-lg rounded-3xl shadow-2xl p-8 m-4 transform transition-all">
        <div class="flex items-center gap-3 mb-6">
            <div class="w-12 h-12 bg-red-100 rounded-xl flex items-center justify-center">
                <svg class="w-6 h-6 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
                </svg>
            </div>
            <div>
                <h2 class="text-2xl font-bold text-gray-900">Decline Request</h2>
                <p class="text-gray-600 text-sm">Provide a reason for declining</p>
            </div>
        </div>

        <form id="declineForm" asp-action="DeclineExpense" asp-controller="Sales_Finance" method="post">
            <input type="hidden" name="id" id="decline-expense-id" value="" />
            
            <div class="mb-6">
                <label class="block text-gray-700 font-medium mb-2">
                    Reason for Decline <span class="text-red-500">*</span>
                </label>
                <textarea name="DeclineReason" 
                          id="decline-reason-textarea" 
                          class="w-full border border-gray-300 rounded-xl p-4 focus:ring-2 focus:ring-red-500 focus:border-transparent transition-all" 
                          rows="4" 
                          required 
                          placeholder="Please provide a clear reason for declining this expense request..."></textarea>
                <p class="text-gray-500 text-xs mt-2">This reason will be sent to the requester</p>
            </div>

            <div class="flex justify-end gap-3 pt-4 border-t border-gray-200">
                <button type="button" id="cancelDeclineBtn" class="px-6 py-2.5 bg-gray-100 text-gray-700 rounded-xl hover:bg-gray-200 transition-colors font-medium">
                    Cancel
                </button>
                <button type="submit" class="px-6 py-2.5 bg-gradient-to-r from-red-500 to-red-600 text-white rounded-xl hover:from-red-600 hover:to-red-700 transition-all shadow-md hover:shadow-lg font-medium flex items-center gap-2">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                    </svg>
                    Confirm Decline
                </button>
            </div>
        </form>
    </div>
</div>


<!-- TRANSFER FUND MODAL -->
<div id="transferFundModal" class="fixed inset-0 bg-white bg-opacity-60 hidden items-center justify-center z-50 backdrop-blur-sm">
    <div class="bg-white w-full max-w-2xl rounded-3xl shadow-2xl p-8 m-4 max-h-[90vh] overflow-y-auto transform transition-all">
        <div class="flex items-center gap-3 mb-6 pb-4 border-b border-gray-200">
            <div class="w-12 h-12 bg-green-100 rounded-xl flex items-center justify-center">
                <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
            </div>
            <div>
                <h2 class="text-2xl font-bold text-gray-900">Approve & Transfer Funds</h2>
                <p class="text-gray-600 text-sm">Complete transfer details to approve this expense</p>
            </div>
        </div>

        <!-- Balance Warning -->
        <div id="insufficientBalanceWarning" class="bg-red-50 border-l-4 border-red-500 p-4 mb-6 rounded-lg hidden">
            <div class="flex items-start gap-3">
                <svg class="w-5 h-5 text-red-600 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
                </svg>
                <div>
                    <p class="text-red-800 font-semibold">Insufficient Balance</p>
                    <p class="text-red-700 text-sm mt-1">Available balance is not sufficient for this transfer. Please add funds before approving.</p>
                </div>
            </div>
        </div>

        <form id="approveForm" asp-action="ApproveExpense" asp-controller="Sales_Finance" method="post">
            <input type="hidden" name="id" id="approve-expense-id" value="" />
            
            <!-- Balance Summary -->
            <div class="grid grid-cols-3 gap-4 mb-6">
                <div class="bg-blue-50 p-4 rounded-xl border border-blue-200">
                    <p class="text-xs text-blue-700 font-medium mb-1">Current Balance</p>
                    <p class="text-lg font-bold text-blue-900">₱50,000.00</p>
                </div>
                <div class="bg-orange-50 p-4 rounded-xl border border-orange-200">
                    <p class="text-xs text-orange-700 font-medium mb-1">Transfer Amount</p>
                    <p class="text-lg font-bold text-orange-900" id="transfer-amount-display-main">₱0.00</p>
                </div>
                <div class="bg-green-50 p-4 rounded-xl border border-green-200">
                    <p class="text-xs text-green-700 font-medium mb-1">Balance After</p>
                    <p class="text-lg font-bold text-green-900" id="balance-after-transfer">₱50,000.00</p>
                </div>
            </div>

            <div class="space-y-5">
                <div>
                    <label class="block text-gray-700 font-medium mb-2">
                        Transfer Amount
                    </label>
                    <input type="text" 
                           id="transfer-amount-display" 
                           class="w-full border border-gray-300 rounded-xl p-4 bg-gray-50 text-gray-700 font-semibold text-lg" 
                           readonly />
                </div>

                <div>
                    <label class="block text-gray-700 font-medium mb-2">
                        Transfer To (Account/Person) <span class="text-red-500">*</span>
                    </label>
                    <input type="text" 
                           name="TransferTo" 
                           class="w-full border border-gray-300 rounded-xl p-4 focus:ring-2 focus:ring-green-500 focus:border-transparent transition-all" 
                           required 
                           placeholder="Enter recipient name or account number" />
                </div>

                <div>
                    <label class="block text-gray-700 font-medium mb-2">
                        Payment Method <span class="text-red-500">*</span>
                    </label>
                    <select name="PaymentMethod" 
                            class="w-full border border-gray-300 rounded-xl p-4 focus:ring-2 focus:ring-green-500 focus:border-transparent transition-all" 
                            required>
                        <option value="">Select payment method</option>
                        <option value="Bank Transfer">Bank Transfer</option>
                        <option value="Cash">Cash</option>
                        <option value="Check">Check</option>
                        <option value="Online Payment">Online Payment</option>
                        <option value="Credit Card">Credit Card</option>
                    </select>
                </div>

                <div>
                    <label class="block text-gray-700 font-medium mb-2">
                        Reference Number
                    </label>
                    <input type="text" 
                           name="ReferenceNumber" 
                           class="w-full border border-gray-300 rounded-xl p-4 focus:ring-2 focus:ring-green-500 focus:border-transparent transition-all" 
                           placeholder="Enter reference or transaction number (optional)" />
                </div>

                <div>
                    <label class="block text-gray-700 font-medium mb-2">
                        Transfer Notes
                    </label>
                    <textarea name="TransferNotes" 
                              class="w-full border border-gray-300 rounded-xl p-4 focus:ring-2 focus:ring-green-500 focus:border-transparent transition-all" 
                              rows="3" 
                              placeholder="Add any additional notes about the transfer (optional)"></textarea>
                </div>
            </div>

            <div class="flex justify-end gap-3 mt-8 pt-6 border-t border-gray-200">
                <button type="button" id="cancelTransferBtn" class="px-6 py-2.5 bg-gray-100 text-gray-700 rounded-xl hover:bg-gray-200 transition-colors font-medium">
                    Cancel
                </button>
                <button type="submit" id="confirmApproveBtn" class="px-6 py-2.5 bg-gradient-to-r from-green-500 to-green-600 text-white rounded-xl hover:from-green-600 hover:to-green-700 transition-all shadow-md hover:shadow-lg font-medium flex items-center gap-2">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                    </svg>
                    Confirm & Approve
                </button>
            </div>
        </form>
    </div>
</div>


<!-- ADD EXPENSE MODAL -->
<div id="expenseModal" class="fixed inset-0 bg-white bg-opacity-60 hidden items-center justify-center z-50 backdrop-blur-sm">
    <div class="bg-white w-full max-w-2xl rounded-3xl shadow-2xl p-8 m-4 max-h-[90vh] overflow-y-auto transform transition-all">
        <div class="flex items-center gap-3 mb-6 pb-4 border-b border-gray-200">
            <div class="w-12 h-12 bg-blue-100 rounded-xl flex items-center justify-center">
                <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
                </svg>
            </div>
            <div>
                <h2 class="text-2xl font-bold text-gray-900">Create New Expense</h2>
                <p class="text-gray-600 text-sm">Submit a new expense request</p>
            </div>
        </div>

        <form asp-action="AddExpense" asp-controller="Sales_Finance" method="post">
            <div class="space-y-5">
                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label class="block text-gray-700 font-medium mb-2">
                            Department <span class="text-red-500">*</span>
                        </label>
                        <select name="Department" 
                                class="w-full border border-gray-300 rounded-xl p-3 focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all" 
                                required>
                            <option value="">Select department</option>
                            <option value="HR">HR</option>
                            <option value="Accounting">Accounting</option>
                            <option value="Website">Website</option>
                            <option value="Marketing">Marketing</option>
                            <option value="Inventory">Inventory</option>
                        </select>
                    </div>
                    <div>
                        <label class="block text-gray-700 font-medium mb-2">
                            Expense Type <span class="text-red-500">*</span>
                        </label>
                        <input name="ExpenseType" 
                               class="w-full border border-gray-300 rounded-xl p-3 focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all" 
                               required 
                               placeholder="e.g., Office Supplies" />
                    </div>
                </div>

                <div class="grid grid-cols-2 gap-4">
                    <div>
                        <label class="block text-gray-700 font-medium mb-2">
                            Requested By <span class="text-red-500">*</span>
                        </label>
                        <input name="RequestedBy" 
                               class="w-full border border-gray-300 rounded-xl p-3 focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all" 
                               required 
                               placeholder="Your name" />
                    </div>
                    <div>
                        <label class="block text-gray-700 font-medium mb-2">
                            Amount (₱) <span class="text-red-500">*</span>
                        </label>
                        <input type="number" 
                               name="Amount" 
                               step="0.01" 
                               class="w-full border border-gray-300 rounded-xl p-3 focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all" 
                               required 
                               placeholder="0.00" />
                    </div>
                </div>

                <div>
                    <label class="block text-gray-700 font-medium mb-2">
                        Description <span class="text-red-500">*</span>
                    </label>
                    <textarea name="Description" 
                              class="w-full border border-gray-300 rounded-xl p-3 focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all" 
                              rows="3" 
                              required 
                              placeholder="Describe the purpose of this expense..."></textarea>
                </div>

                <div>
                    <label class="block text-gray-700 font-medium mb-2">
                        Additional Notes
                    </label>
                    <textarea name="Notes" 
                              class="w-full border border-gray-300 rounded-xl p-3 focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all" 
                              rows="2" 
                              placeholder="Any additional information (optional)"></textarea>
                </div>
            </div>

            <div class="flex justify-end gap-3 mt-8 pt-6 border-t border-gray-200">
                <button type="button" id="closeModalBtn" class="px-6 py-2.5 bg-gray-100 text-gray-700 rounded-xl hover:bg-gray-200 transition-colors font-medium">
                    Cancel
                </button>
                <button type="submit" class="px-6 py-2.5 bg-gradient-to-r from-blue-600 to-blue-700 text-white rounded-xl hover:from-blue-700 hover:to-blue-800 transition-all shadow-md hover:shadow-lg font-medium flex items-center gap-2">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                    </svg>
                    Submit Request
                </button>
            </div>
        </form>
    </div>
</div>

<script>
    let currentExpenseId = null;
    let currentExpenseAmount = null;
    const AVAILABLE_BALANCE = 50000; // This should be dynamic from backend

    // Helper function to format currency
    function formatCurrency(amount) {
        return '₱' + parseFloat(amount).toLocaleString('en-PH', { minimumFractionDigits: 2, maximumFractionDigits: 2 });
    }

    // Helper function to calculate balance after approval
    function calculateBalanceAfter(expenseAmount) {
        const amount = parseFloat(expenseAmount.replace(/,/g, ''));
        return AVAILABLE_BALANCE - amount;
    }

    // Check if balance is sufficient
    function checkBalance(expenseAmount) {
        const amount = parseFloat(expenseAmount.replace(/,/g, ''));
        return AVAILABLE_BALANCE >= amount;
    }

    // Show low balance alert
    function checkLowBalance() {
        const alertDiv = document.getElementById('balanceAlert');
        if (AVAILABLE_BALANCE < 20000) {
            alertDiv.classList.remove('hidden');
        }
    }

    // Initialize page
    document.addEventListener('DOMContentLoaded', function() {
        checkLowBalance();
    });

    // Add Expense Modal - Show
    document.getElementById("openModalBtn").addEventListener("click", function () {
        document.getElementById("expenseModal").classList.remove("hidden");
        document.getElementById("expenseModal").classList.add("flex");
    });

    // Add Expense Modal - Close
    document.getElementById("closeModalBtn").addEventListener("click", function () {
        document.getElementById("expenseModal").classList.add("hidden");
        document.getElementById("expenseModal").classList.remove("flex");
    });

    // Preview Modal - Open and populate with expense data
    document.querySelectorAll(".view-details-btn").forEach(button => {
        button.addEventListener("click", function () {
            // Get data from button attributes
            const expenseId = this.getAttribute("data-expense-id");
            const requester = this.getAttribute("data-requester");
            const department = this.getAttribute("data-department");
            const type = this.getAttribute("data-type");
            const amount = this.getAttribute("data-amount");
            const description = this.getAttribute("data-description");
            const notes = this.getAttribute("data-notes") || "No additional notes provided";
            const status = this.getAttribute("data-status");
            const id = this.getAttribute("data-id");

            // Store for later use
            currentExpenseId = id;
            currentExpenseAmount = amount;

            // Calculate balance after approval
            const balanceAfter = calculateBalanceAfter(amount);

            // Populate preview modal
            document.getElementById("preview-expense-id").textContent = expenseId;
            document.getElementById("preview-requester").textContent = requester;
            document.getElementById("preview-requester-initial").textContent = requester.substring(0, 1).toUpperCase();
            document.getElementById("preview-department").textContent = department;
            document.getElementById("preview-type").textContent = type;
            document.getElementById("preview-amount").textContent = "₱" + amount;
            document.getElementById("preview-description").textContent = description;
            document.getElementById("preview-notes").textContent = notes;
            document.getElementById("preview-status").textContent = status;
            document.getElementById("balance-after-approval").textContent = formatCurrency(balanceAfter);

            // Show preview modal
            document.getElementById("previewModal").classList.remove("hidden");
            document.getElementById("previewModal").classList.add("flex");
        });
    });

    // Preview Modal - Close
    document.getElementById("closePreviewBtn").addEventListener("click", function () {
        document.getElementById("previewModal").classList.add("hidden");
        document.getElementById("previewModal").classList.remove("flex");
    });

    document.getElementById("cancelPreviewBtn").addEventListener("click", function () {
        document.getElementById("previewModal").classList.add("hidden");
        document.getElementById("previewModal").classList.remove("flex");
    });

    // Approve Button - Show Transfer Fund Modal
    document.getElementById("approveBtn").addEventListener("click", function () {
        // Close preview modal
        document.getElementById("previewModal").classList.add("hidden");
        document.getElementById("previewModal").classList.remove("flex");

        // Set expense ID and amount
        document.getElementById("approve-expense-id").value = currentExpenseId;
        document.getElementById("transfer-amount-display").value = "₱" + currentExpenseAmount;
        document.getElementById("transfer-amount-display-main").textContent = "₱" + currentExpenseAmount;

        // Calculate balance after transfer
        const balanceAfter = calculateBalanceAfter(currentExpenseAmount);
        document.getElementById("balance-after-transfer").textContent = formatCurrency(balanceAfter);

        // Check if balance is sufficient
        const isSufficient = checkBalance(currentExpenseAmount);
        const warningDiv = document.getElementById("insufficientBalanceWarning");
        const submitBtn = document.getElementById("confirmApproveBtn");

        if (!isSufficient) {
            warningDiv.classList.remove("hidden");
            submitBtn.disabled = true;
            submitBtn.classList.add("opacity-50", "cursor-not-allowed");
            // Update balance after color to red
            document.getElementById("balance-after-transfer").classList.add("text-red-600");
            document.getElementById("balance-after-transfer").classList.remove("text-green-900");
        } else {
            warningDiv.classList.add("hidden");
            submitBtn.disabled = false;
            submitBtn.classList.remove("opacity-50", "cursor-not-allowed");
            // Update balance after color
            if (balanceAfter < 20000) {
                document.getElementById("balance-after-transfer").classList.add("text-yellow-600");
                document.getElementById("balance-after-transfer").classList.remove("text-green-900");
            } else {
                document.getElementById("balance-after-transfer").classList.add("text-green-900");
                document.getElementById("balance-after-transfer").classList.remove("text-yellow-600");
            }
        }

        // Show transfer fund modal
        document.getElementById("transferFundModal").classList.remove("hidden");
        document.getElementById("transferFundModal").classList.add("flex");
    });

    // Transfer Fund Modal - Cancel
    document.getElementById("cancelTransferBtn").addEventListener("click", function () {
        document.getElementById("transferFundModal").classList.add("hidden");
        document.getElementById("transferFundModal").classList.remove("flex");
        
        // Clear form
        document.getElementById("approveForm").reset();
    });

    // Decline Button - Show Decline Reason Modal
    document.getElementById("declineBtn").addEventListener("click", function () {
        // Close preview modal
        document.getElementById("previewModal").classList.add("hidden");
        document.getElementById("previewModal").classList.remove("flex");

        // Set expense ID
        document.getElementById("decline-expense-id").value = currentExpenseId;

        // Show decline reason modal
        document.getElementById("declineReasonModal").classList.remove("hidden");
        document.getElementById("declineReasonModal").classList.add("flex");
    });

    // Decline Reason Modal - Cancel
    document.getElementById("cancelDeclineBtn").addEventListener("click", function () {
        document.getElementById("declineReasonModal").classList.add("hidden");
        document.getElementById("declineReasonModal").classList.remove("flex");
        
        // Clear form
        document.getElementById("declineForm").reset();
    });
    
</script>



<!-- Script -->
<script>
    document.addEventListener("DOMContentLoaded", function () {
        const button = document.getElementById("filterButton");
        const dropdown = document.getElementById("filterDropdown");

        // Toggle dropdown visibility
        button.addEventListener("click", function (e) {
            e.stopPropagation();
            dropdown.classList.toggle("hidden");
        });

        // Close dropdown when clicking outside
        document.addEventListener("click", function (e) {
            if (!dropdown.contains(e.target) && !button.contains(e.target)) {
                dropdown.classList.add("hidden");
            }
        });
    });
</script>
